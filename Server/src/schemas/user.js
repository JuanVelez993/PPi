'use strict'

/*const Joi = require('joi')

const createUser = Joi.object().keys({
  name: Joi.string().required('El campo nombre es obligatorio'),
  lastName: Joi.string(),
  email: Joi.string().email()
})

module.exports = {
  createUser
}*/