'use strict'
const hbs = require('hbs');

function registerHelpers() {
   /* hbs.registerHelper("modal", function(mensaje) {
        popupS.alert({
            content: 'Hello'
        });
      });*/
}

module.exports = {
    registerHelpers
}